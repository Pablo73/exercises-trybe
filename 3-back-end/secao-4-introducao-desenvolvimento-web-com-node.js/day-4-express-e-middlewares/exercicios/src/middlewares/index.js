const { validationName, ifExistName } = require('./validationKeyName');
const validationPrice = require('./validationprice');

module.exports = {
    validationName,
    ifExistName,
    validationPrice,
};